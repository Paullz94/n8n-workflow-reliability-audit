{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://northstar.local/make-reliability/intake.schema.json",
  "title": "Northstar Make Data Integrity Audit Intake",
  "type": "object",
  "additionalProperties": false,
  "required": ["scenario_name", "business_goal"],
  "properties": {
    "scenario_name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 160,
      "description": "Human-readable scenario label. Do not include customer secrets."
    },
    "business_goal": {
      "type": "string",
      "minLength": 1,
      "maxLength": 500,
      "description": "What must reliably happen when the scenario succeeds."
    },
    "critical_side_effects": {
      "type": "array",
      "maxItems": 10,
      "items": {"type": "string", "minLength": 1, "maxLength": 160},
      "description": "External writes/actions whose duplication, omission or corruption matters."
    },
    "duplicate_tolerance": {
      "type": "string",
      "enum": ["none", "limited", "unknown"],
      "default": "unknown"
    },
    "ordering_required": {
      "type": "boolean",
      "default": false
    },
    "recovery_expectation": {
      "type": "string",
      "maxLength": 500,
      "description": "Expected behavior after a transient/permanent downstream failure."
    }
  }
}
